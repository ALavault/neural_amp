# M5 - Final Campaign Status

## Outcome

M5 was not entered. The M4 smoke gate failed its quality, efficiency, and
antialiasing conditions, and the preregistered equal-parameter recovery also
failed its stop rule. Starting the 76-run maturation matrix would therefore
violate the phase order and uncontrolled-expansion rule.

## Preserved evidence

- 24 completed M4 physical runs across two devices, four models, and three seeds.
- Two completed equal-parameter recovery runs.
- Official A2 C++ parity and host CPU reference.
- Python streaming cost diagnostics for B2, S3, and S4.
- Synthetic S0-S4 mechanism tests and metric validation.

## External evaluation

No dataset was assigned to `EXTERNAL_REPORT_ONLY`, no external model output was
generated or inspected, and `external_retest_authorized` remains `false`. The
post-campaign lock records the negative internal protocol; it is not a
confirmatory preregistration.

## Disposition

Pivot P4 is active. The campaign proceeds to M6 as a transparent reproducible
negative result and benchmark, not as a successful model claim.
