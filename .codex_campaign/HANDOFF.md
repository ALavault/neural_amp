# Handoff

## Current position

M0 bootstrap is in progress. The workspace began empty and without Git. Official NAM sources are pinned as submodules, the repository skeleton exists, and the external evaluation lock is closed.

## Resume action

Run `make bootstrap`, then `make lint`, `make test`, `make data-audit`, and `make smoke`. If all pass, register the identity artifact, update the M0 report/state, and create the first local commit before beginning M1.

## Absolute blockers

None for M0 or synthetic M1. Physical paired data and final dataset licenses remain unresolved but do not block current work.

