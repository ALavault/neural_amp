schema_version: 1
campaign_version: AMP-QUALITY-ARCH-v1
family: selective_s6_x2
aa_mode: full_island_x2
observer_outside_x2: false
deployable:
  training_eligible: false
  profile_selection: largest_profile_passing_blind_native_skeleton_gate
  selected_profile: max
  selection_evidence: .codex_campaign/amp_quality_arch_v1/NATIVE_SKELETON.json
  profiles:
    slim: {state_dim: 48}
    balanced: {state_dim: 64}
    full: {state_dim: 96}
    max: {state_dim: 128}
teacher:
  eligible: false
  rejection: projected_training_time_exceeds_per_trajectory_cap
  state_dim: 256
runtime:
  sample_rate_hz: 48000
  internal_sample_rate_hz: 96000
  latency_samples: 32
  precision: float32
