# FSSR-R2-48K-v2 decision log

Entries are append-only. Corrections must name the superseded entry.

## 2026-08-28 — R2-48K-V2-D-001 — New version, immutable invalid parent

Create `FSSR-R2-48K-v2`; do not reopen or amend v1. Preserve its partial file,
failure, ledger event and `INVALID` verdict. Inherit every scientific threshold,
fixture, route, probe, split, holdout rule and claim boundary.

## 2026-08-28 — R2-48K-V2-D-002 — Canonical extended-real transport

Represent diagnostic `-inf`, `+inf` and `NaN` as tagged standard-JSON objects.
An exact zero ratio remains mathematically negative infinity in dB and is never
clipped to a favorable finite floor. Decision aggregates must remain finite;
`NaN` invalidates evidence before the gate. Serialize, parse and validate the
complete evidence in memory before creating its immutable file.

## 2026-08-28 — R2-48K-V2-D-003 — Single prospective rerun

Authorize one new v2 synthetic matrix only after transport preflight. No v1
numeric value is reused and no result-dependent v2 retry is allowed. Continue to
screening only on a literal passing x2 gate.

## 2026-08-28 — R2-48K-V2-D-004 — Preserve the invalid mechanism outcome

The one authorized v2 matrix completed and serialized successfully. The frozen
gate rejected `tanh/full_island_x2` because its aggregate anti-silence guard was
false, so x2 was not evaluated. Record `INVALID`, keep all downstream stages
locked, and do not reinterpret the run as `NO-GO` or alter the relative complex
harmonic-error guard after observing the evidence.

The postmortem may report that the first failing row's other guard checks passed
and that the relative error denominator is numerically ill-conditioned near an
almost exact AA-off reference. This diagnosis is not a gate result and cannot be
used to resume v2.
