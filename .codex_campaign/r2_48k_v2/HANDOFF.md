# FSSR-R2-48K-v2 handoff

The lineage is terminal with verdict `INVALID`. Preflight and the metadata-only
data audit passed; the single authorized mechanism matrix completed, but its
unchanged anti-silence guard failed before x2 evaluation. Do not rerun the v2
matrix, run screening, open Blackstar/UA1176 outputs, access
`EXTERNAL_REPORT_ONLY`, or reinterpret postmortem metrics as a scientific gate.

Any continuation requires a new prospective lineage. It must define and test a
well-conditioned complex-harmonic guard before measurements, for example a
relative comparison with an explicit absolute-error indifference region. Its
threshold must be justified independently of the observed v2 values. V1 and v2
artifacts, failures and terminal verdicts remain immutable.
